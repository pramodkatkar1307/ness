import { ExitstatusPipe } from './exitstatus.pipe';

describe('ExitstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ExitstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
